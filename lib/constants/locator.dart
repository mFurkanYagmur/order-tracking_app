/*
import 'package:get_it/get_it.dart';
import 'package:order_tracking_app/repository/auth_repository.dart';
import 'package:order_tracking_app/data/preferences_helper.dart';
import 'package:order_tracking_app/repository/order_repository.dart';

import '../data/firebase_client.dart';

GetIt locator = GetIt.instance;

void setupLocator() {
  locator.registerSingleton(() => FirebaseClient());
  locator.registerSingleton(() => PreferencesHelper());
  locator.registerSingleton(() => AuthRepository());
  locator.registerSingleton(() => OrderRepository());
}
*/
